# Source Material Critical Review

**Engagement:** MediPath Diagnostics (VS-MPD-26-041)
**Reviewer note:** This review reads Sarah Chen's raw technical findings against the MediPath client-context dossier before any recalibration is performed. No scores are changed, no findings are retired or merged, and no attack chains are built here. The purpose is to record a first-pass technical impression, flag where confidence is high versus where verification is needed, and list the specific context elements that Task 3's environmental recalibration will need to test each rating against.

| Finding | First impression | Zone of doubt or confidence | To investigate in Task 3 |
| --- | --- | --- | --- |
| F-01 | Sarah demonstrated a clean tenant-isolation failure on the results API: swapping a result identifier across synthetic labs returned another tenant's health data with a 200 response. The technical proof (two paired requests) is unambiguous. | High confidence in the technical observation itself; the base rating (8.1 High) looks defensible on the evidence shown. The open question is not whether it happened but how large the practical exposure is across MediPath's full laboratory base. | The 400-laboratory scale, the sensitivity of the returned fields (patient identity, test, status), whether the CDN/WAF or SIEM provides any anomaly detection on cross-tenant result requests, and how easily a real result identifier could be guessed or harvested outside the synthetic test conditions. |
| F-02 | A stored, unencoded field in laboratory case notes executed a harmless marker when a support analyst opened the case — a classic stored-injection pattern in a support-facing view. | Reasonably confident in the technical mechanism; less certain about real-world impact, since Sarah's proof intentionally avoided retaining credential material and the CSP is still in report-only mode. | Whether MFA and managed workstations for support staff meaningfully reduce exploitation likelihood, how many laboratory tenants a single support analyst can reach from one session, and the actual enforcement state and rollout timeline for the CSP. |
| F-03 | A long-lived hospital connector token with a broad `integration:all` scope was accepted outside its assigned hospital relationship — a scope and lifetime design flaw rather than a one-off bug. | High confidence this is a genuine architectural weakness; the "High" rating (8.8) seems consistent with a credential that bridges institutional trust boundaries, but the private network's role in limiting exposure isn't yet weighed. | Which network controls actually restrict use of a valid-but-misused token, whether rotation is operationally enforced or only documented, and whether authorization state for connectors is shared with the same identity-cache mechanism implicated in F-09. |
| F-04 | Sarah found `verify_peer=false` in a legacy connector worker's configuration — on its face a serious TLS validation gap on a component handling hospital data. | This is one of the two findings Sarah's handwritten note flagged directly. Her own uncertainty is a signal worth taking seriously, not a scored conclusion: she never tested live interception because the Rules of Engagement barred interference with the managed connectivity layer, so the finding rests on configuration review alone. | Whether the worker can ever reach the public network directly or only through the managed mTLS egress gateway, whether that gateway enforces certificate validation independently and fails closed, and what monitoring exists if the gateway control were bypassed or disabled. |
| F-05 | The sample-status webhook accepts state-changing events based on source-IP allowlisting alone, with no message signature — network trust is being used as a substitute for message-origin authentication. | Confident in the technical gap between "trusted network" and "authenticated message." Less certain how consequential a forged status transition actually is until its downstream effects are understood. | Whether a forged `validated` status can trigger result release or export workflows without further checks, how tightly scoped the partner IP allowlist is, and whether the sample-status workflow's integrity requirement (rated very high in the asset table) changes the practical severity of a spoofed event. |
| F-06 | Export packages documented to expire in 24 hours remained downloadable after 29 days through their original signed link — a retention-policy and access-control mismatch, not a storage-encryption problem. | Confident the mismatch is real and reproducible from the proof given. Uncertain how this maps to MediPath's DPO-approved retention posture and whether it constitutes a compliance gap versus a purely technical one. | The DPO-approved retention policy for exports, whether storage access is logged and reviewed, export volume and health-data sensitivity, and whether the mismatch has implications for HDS or RGPD accountability ahead of the renewal audit. |
| F-07 | Sarah rated a Support Manager's ability to run a bulk, multi-laboratory patient export as Critical (9.1) — on the surface, a broad role holding a very sensitive capability. | This is the second finding Sarah's note flagged, and for a specific reason: the client context describes this as an approved emergency/regulatory workflow with DPO ticketing, WebAuthn step-up, dual approval, case-key encryption, short expiry, and immutable logging — controls the engagement's test role was pre-approved to exercise. The doubt is real but not yet resolved; whether this is a live finding or intended, adequately controlled functionality is exactly what needs verification, not assumed either way. | Whether every production path to this function actually requires the DPO ticket and dual approval or whether some route bypasses them, whether a ticket identifier is technically mandatory versus optional, and whether the behavior observed under the pre-approved test role matches what a real Support Manager session can do unsupervised. |
| F-08 | The password-recovery flow returns different responses for registered versus unregistered accounts, and rate limiting is IP-based rather than account-based — a standard account-enumeration pattern. | Confident in the mechanism and its general classification as a Medium-severity issue; less certain how much it matters given that most laboratory users authenticate through SSO. | The proportion of laboratory users still on local accounts versus SSO/MFA, whether enumerated identities have any downstream use in support-desk social engineering, and whether monitoring exists for recovery-endpoint probing. |
| F-09 | A downgraded administrator retained write privileges for roughly 27 minutes because authorization relies on a time-based distributed cache rather than immediate revocation. | Confident in the technical observation and the design gap it points to (revocation is time-based, not event-driven). Less certain how it should be weighed against F-03, since both may share an identity/authorization-cache root cause. | The frequency of privileged role changes in production, whether privileged actions are separately monitored or approved during the exposure window, and whether resolving the shared cache-invalidation gap would also affect F-03. |
| F-10 | An internal analytics service trusts a caller-supplied `X-Lab-ID` header instead of deriving tenant identity from a verified claim, and is reachable from the application network without going through the gateway. | Confident this is a genuine trust-boundary gap between the gateway and an internal service. Less certain about its practical severity, since the affected data is aggregate analytics rather than individual patient records. | Which service identities can actually reach the analytics endpoint from the internal application network, whether F-03's broad connector token could reach this same path, and whether the asset table's "medium to high depending on granularity" rating for aggregate analytics changes the confidentiality weighting here. |

## Notes on scope and discipline

- This table records first impressions and open questions only. No finding above has been rescored, retired, merged, or connected into an attack chain — that work belongs to later tasks in the engagement (environmental recalibration in Task 3, and false-positive/duplicate-root-cause triage after that).
- F-04 and F-07 carry Sarah's explicit handwritten doubt. Both are treated here as **flagged for careful verification**, not as findings that are already wrong, already confirmed, or already resolved. The client-context elements that could shift either rating are listed above and will be tested directly in the recalibration.
- Confidence in the *technical* observation (what was proven in each proof-of-concept) is generally high across all ten findings; the open questions are consistently about *environmental* weight — asset criticality, compensating controls, regulatory exposure, and operational consequence — which is exactly what the client-context dossier is for.
